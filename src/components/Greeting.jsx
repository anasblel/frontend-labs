export default function Greeting() {
  return (
    <div>
      <h1>Welcome to React!</h1>
      <h2>This is my first component</h2>
      <p>It uses something called the Virtual DOM — a lightweight copy of the real DOM. When state changes</p>
    </div>
  );
}
